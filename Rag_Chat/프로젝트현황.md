# 프로젝트 현황 / Project Status (2026-02-03)
Palantir 제출용. 동작·이슈·계획을 간단히 기록합니다.

## Working Now / 동작 중
- Streamlit UI → Django API → vector search (FAISS/Chroma) → Gemini/Qwen LLM answer  
- Logs: `Chat`, `SearchLog` store question/context/answer  
- Redis session + activity update API  
- Provider presets via API + Streamlit toggle  
- Local one-shot: `run_local_fixed.sh` or docker-compose

## Issues / 주요 이슈
- No auth/RBAC/forbidden-term filter (assumes open endpoints)  
- Redis TTL vs DB `expired_datetime` mismatch → session expiry hard to reproduce  
- No streaming; slow responses hurt UX  
- No monitoring/healthcheck/APM  
- No multi-node or autoscale design

## Near-term / 단기 계획
1) Align session expiry (Redis TTL ↔ DB field) and log expirations  
2) Basic health checks: LLM/Redis/DB ping with warning logs  
3) Add Django middleware hook for forbidden terms/roles, seed a base ruleset  
4) Minimal monitoring: log latency for Celery + LLM calls  
5) Reranker option: plug bge-reranker/Cohere and measure quality

## Mid-term / 중기 검토
- Migrate to Postgres with scripts  
- Streaming/SSE to reduce perceived latency  
- Provider health check + fallback to default (Gemini) on failure
