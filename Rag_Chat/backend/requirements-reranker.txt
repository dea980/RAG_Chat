# Optional ONNX cross-encoder reranker dependencies.
#
# These are intentionally kept out of requirements.txt because optimum pulls in
# torch, and recent Linux/aarch64 torch wheels pull large CUDA dependency wheels.
optimum[onnxruntime]>=1.20.0
transformers>=4.29
torch>=1.11
